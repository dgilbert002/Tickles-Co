# 2026-05-03 — Five-Steps Closeout Handoff

> Generated by Architect mode at 2026-05-03T10:26 UTC.
> Authorized by user directive: **"go for it. lets get all the way to the end"** (after the implementation-progress report covering INTELLIGENCE_UNIFIED_PLAN.md + Phase X plans + PHASE_Y v2).

---

## §0 — TL;DR

Six artefacts in this session, in order:

1. **Step 1** — Dashboard tab recon: News Feed (X.4), Drawer (X.5), Config (X.6) confirmed **NOT SHIPPED** (zero matches in [`shared/dashboard/`](shared/dashboard/:1)).
2. **Step 2** — PHASE_Y v2 §11 surfaced: 5 open questions awaiting user decision before Y.0 can begin.
3. **Step 3** — Phase R closeout spec: ~57 % shipped (3 Python scripts + cron_canary + heartbeat + migration), 43 % missing (GH Actions workflow + 4 tests + 3 systemd units + 2 schema snapshots + Makefile targets).
4. **Step 4** — [`shared/docs/INTELLIGENCE_UNIFIED_PLAN.md`](shared/docs/INTELLIGENCE_UNIFIED_PLAN.md:3308) reconciled: Phase L purpose now carries supersedure banner; benchmark checklist marks 18 of 25 items shipped; Phase R purpose carries partial-shipped status banner.
5. **Step 5** — [`CLAUDE.md`](CLAUDE.md:360) §"Intelligence Pipeline" extended with status blocks for Phases 6-11, Phase L, Phase R partial, Phase X (F1-F11), Phase Y v2 design.
6. **Step 6** — This handoff.

---

## §1 — Step 1 closeout: Dashboard tab recon

**Method:** four searches against [`shared/dashboard/`](shared/dashboard/:1):

| Pattern | Hits | Verdict |
|---------|------|---------|
| `news_feed\|news-feed\|NewsFeed\|/news\b` | 0 | X.4 unshipped |
| `interpretation_drawer\|InterpretationDrawer\|drawer` | 0 | X.5 unshipped |
| `config_tab\|ConfigTab\|/config\b` | 0 | X.6 unshipped |
| `data-tab` (in [`web/index.html`](shared/dashboard/web/index.html:52)) | 7 hits = `overview, leaderboard, signals, positions, interpretations, queue, trader-drill` | confirms only existing tabs |

**Live tab inventory ([`shared/dashboard/web/index.html`](shared/dashboard/web/index.html:52-60)):**
- Overview, Leaderboard, Signals, Positions, Interpretations, Live Queue (visible)
- Trader Drill (registered but hidden)

**API routes ([`shared/dashboard/server.py`](shared/dashboard/server.py:1)):**
`/api/snapshot, /api/services, /api/leaderboard, /api/signals, /api/positions, /api/interpretations, /api/trader-drill, /api/charts/{interp_id}, /api/media/{media_id}` + `/ws/queue`.

**Auth:** Telegram-OTP, default-deny via [`shared/dashboard/auth.py`](shared/dashboard/auth.py:1), CSRF via [`shared/dashboard/csrf.py`](shared/dashboard/csrf.py:1), rate-limit via [`shared/dashboard/rate_limit.py`](shared/dashboard/rate_limit.py:1).

**X.4-X.6 status:** ❌ ❌ ❌ — all three sub-phases definitively NOT STARTED. X.7 (E2E smoke) is N/A until X.4-X.6 land.

---

## §2 — Step 2 closeout: PHASE_Y v2 §11 open questions (verbatim)

From [`shared/docs/PHASE_Y_LEARNING_DASHBOARD_PLAN.md`](shared/docs/PHASE_Y_LEARNING_DASHBOARD_PLAN.md:485) §11:

> 1. **Skill-score weight calibration (§3.1)** — the 0.30 / 0.25 / 0.20 / 0.15 / 0.10 split is judgement, not data-driven. Confirm we ship with this and recalibrate after 30d of data, OR pick different weights?
> 2. **C4 mem0 recall threshold** — what counts as a "match"? v2 proposes "memory metadata.outcome equals position outcome". Is this acceptable, or does the user want a richer match (e.g. dimension-and-outcome-and-symbol)?
> 3. **Window default** — three views are 7d / 14d / 30d. Default tab is 7d. Confirm, or default to 14d (more stable)?
> 4. **Failed-trade threshold** — §9 says "loss = realized_pnl_usd_final < 0". Should breakeven (-$1 to +$1) be a separate bucket?
> 5. **Coach A/A seed (§7 issue v)** — confirm we burn 2 weeks of LLM spend on identical-prompt A/A to break the deadlock? Estimated cost based on F3's $1.137 over 77 trades = ~$0.015 per call × 2× variants × 14 days × ~5 trades/day ≈ $2-3. Tiny. Worth it?

**No Y.* sub-phase can start until these answers are recorded.** Recommended capture path: a single user reply, then Architect mode amends [`shared/docs/PHASE_Y_LEARNING_DASHBOARD_PLAN.md`](shared/docs/PHASE_Y_LEARNING_DASHBOARD_PLAN.md:1) §11 with answers, then Code mode begins Y.0 (MemU enum extension).

---

## §3 — Step 3 closeout: Phase R closeout spec

### §3.1 Already shipped (verified 2026-05-03)

| File | Purpose |
|------|---------|
| [`shared/scripts/schema_diff.py`](shared/scripts/schema_diff.py:1) | Schema-drift detector — compares live DB to canonical snapshot |
| [`shared/scripts/writer_registry_grep.py`](shared/scripts/writer_registry_grep.py:1) | Writer-domain enforcer — fails CI on unauthorized `INSERT/UPDATE/DELETE` |
| [`shared/scripts/master_sync.py`](shared/scripts/master_sync.py:1) | Master-schema sync gate |
| [`shared/intelligence/heartbeat.py`](shared/intelligence/heartbeat.py:1) | `record_heartbeat()` helper for daemons |
| [`shared/intelligence/cron_canary.py`](shared/intelligence/cron_canary.py:1) | Watchdog daemon — alerts on stale heartbeats |
| [`shared/intelligence/migrations/2026_05_04_phase_r_cron_heartbeats.sql`](shared/intelligence/migrations/2026_05_04_phase_r_cron_heartbeats.sql:1) | `cron_heartbeats` table migration |
| [`shared/dashboard/server.py:313`](shared/dashboard/server.py:313) | `handle_services` already reads `cron_heartbeats` (partial wire-in) |

### §3.2 Closeout artefacts (missing — order of operations)

**Priority 1 — CI gate (blocks Phase L benchmark "[BI] No writes from dashboard"):**

```yaml
# .github/workflows/schema-and-writer-gates.yml  (NEW)
name: Schema & Writer Gates
on: [pull_request, push]
jobs:
  schema-diff:
    runs-on: ubuntu-latest
    services:
      postgres:
        image: postgres:16
        env:
          POSTGRES_PASSWORD: postgres
        options: >-
          --health-cmd pg_isready
        ports: [5432:5432]
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with: { python-version: "3.12" }
      - run: pip install -r requirements.txt
      - run: psql -h localhost -U postgres -f shared/migration/tickles_shared_pg.sql
      - run: python shared/scripts/schema_diff.py --against shared/scripts/snapshots/tickles_shared.snapshot.sql --fail-on-drift
  writer-registry:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with: { python-version: "3.12" }
      - run: python shared/scripts/writer_registry_grep.py --strict
  master-sync:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: python shared/scripts/master_sync.py --check-only
```

**Priority 2 — Tests (4 missing):**
- `shared/tests/test_schema_diff.py` — fixture: tweak a column type, assert drift detected
- `shared/tests/test_writer_registry_grep.py` — fixture: synthesise a forbidden `INSERT INTO tracked_positions` from `dashboard.*`, assert non-zero exit
- `shared/tests/test_master_sync_gate.py` — fixture: per-company DB diverges from master, assert gate fails
- `shared/tests/test_cron_canary.py` — fixture: insert `cron_heartbeats` row with `last_beat_at = now() - interval '2 hours'`, assert canary alerts

**Priority 3 — Systemd units (3 missing):**

```ini
# systemd/tickles-schema-drift.timer
[Unit]
Description=Tickles schema drift check (every 6h)
[Timer]
OnCalendar=*-*-* 00,06,12,18:00:00
Persistent=true
[Install]
WantedBy=timers.target

# systemd/tickles-schema-drift.service
[Unit]
Description=Tickles schema drift checker
After=network.target
[Service]
Type=oneshot
WorkingDirectory=/opt/tickles
ExecStart=/usr/bin/python3 -m shared.scripts.schema_diff --alert-only

# systemd/tickles-cron-canary.service
[Unit]
Description=Tickles cron heartbeat watchdog
After=network.target postgresql.service
[Service]
Type=simple
Restart=always
WorkingDirectory=/opt/tickles
ExecStart=/usr/bin/python3 -m shared.intelligence.cron_canary
[Install]
WantedBy=multi-user.target
```

**Priority 4 — Schema snapshots:**
- `shared/scripts/snapshots/tickles_shared.snapshot.sql` — `pg_dump --schema-only` of `tickles_shared.public`
- `shared/scripts/snapshots/tickles_company.snapshot.sql` — `pg_dump --schema-only` of any one `tickles_<company>.public`

**Priority 5 — Makefile targets:**

```makefile
refresh-snapshots:
	pg_dump --schema-only --no-owner -h localhost -U postgres tickles_shared > shared/scripts/snapshots/tickles_shared.snapshot.sql
	pg_dump --schema-only --no-owner -h localhost -U postgres tickles_rubicon > shared/scripts/snapshots/tickles_company.snapshot.sql

gate-local:
	python shared/scripts/schema_diff.py --against shared/scripts/snapshots/tickles_shared.snapshot.sql
	python shared/scripts/writer_registry_grep.py --strict
	python shared/scripts/master_sync.py --check-only
```

**Priority 6 — Heartbeat wire-in:** add `await record_heartbeat("postmortem")` (etc.) at the end of `tick()` in:
- [`shared/intelligence/postmortem_service.py`](shared/intelligence/postmortem_service.py:666) `tick()`
- [`shared/intelligence/edge_scorer_service.py`](shared/intelligence/edge_scorer_service.py:1) tick loop
- [`shared/intelligence/coach_service.py`](shared/intelligence/coach_service.py:1) tick loop
- [`shared/intelligence/chart_hacker_opinion_service.py`](shared/intelligence/chart_hacker_opinion_service.py:1) `_process_position()` exit

**Estimate:** 1 day Code mode (workflow + 4 tests + 3 unit files + snapshots + Makefile + 4 heartbeat wire-ins).

---

## §4 — Step 4 closeout: INTELLIGENCE_UNIFIED_PLAN.md reconciliation

**Two diffs applied to [`shared/docs/INTELLIGENCE_UNIFIED_PLAN.md`](shared/docs/INTELLIGENCE_UNIFIED_PLAN.md:1):**

### §4.1 — Phase L Purpose banner (around line 3308)

Inserted a "STATUS (2026-05-03): MOSTLY SHIPPED, SUPERSEDED FOR FORWARD WORK" callout pointing readers to:
- [`shared/docs/PHASE_X_DASHBOARD_AND_PIPELINE_PLAN.md`](shared/docs/PHASE_X_DASHBOARD_AND_PIPELINE_PLAN.md:1) for X.0-X.7 (with current shipped/unshipped status per sub-phase)
- [`shared/docs/PHASE_Y_LEARNING_DASHBOARD_PLAN.md`](shared/docs/PHASE_Y_LEARNING_DASHBOARD_PLAN.md:1) for Learning tab + skill-vs-luck composite

### §4.2 — Phase L Benchmark checklist (around line 3555)

Marked **18 of 25** items shipped (`[x]`), **6 deferred** (`[ ]`), **1 in progress** (`[-]`):

| Status | Item |
|--------|------|
| `[x]` | Dashboard reachable over Tailscale |
| `[x]` | All 7 routes return 200 with valid OTP |
| `[x]` | All 7 routes return 401 without session |
| `[x]` | Stats overview strip on every page |
| `[x]` | Leaderboard from `actor_leaderboard` view |
| `[x]` | Signals tab links to Discord/Telegram + opticals |
| `[x]` | Positions card shows trader/llm/agent/postmortem |
| `[x]` | Interpretations inline-renders chart + JSON |
| `[x]` | WebSocket pushes within 5s |
| `[x]` | Anchor-link round-trip works |
| `[ ]` | Anchor preserved through OTP redirect — *deferred, not yet verified* |
| `[x]` | Aggregator: new company = zero code change |
| `[ ]` | Writer-registry CI gate listed `dashboard` as non-writer — *blocked on Phase R closeout* |
| `[x]` | Chart renderer caches |
| `[x]` | Chart cache invalidates on `prompt_version` bump |
| `[x]` | Live Queue shows trader + agent trades |
| `[x]` | Trader Drill side-by-side charts |
| `[x]` | Trader Drill free-form chips |
| `[x]` | Cost overview strip sums every provider |
| `[ ]` | `/api/services` shows `kind='agent_cron'` next-fire — *partial: services endpoint exists, kind not rendered* |
| `[x]` | Mobile portrait collapses cleanly |
| `[ ]` | No N+1 queries — *not benchmarked* |
| `[x]` | Empty-state pages render correctly |
| `[ ]` | WS hard cap 51st rejected — *not enforced* |
| `[ ]` | `pytest` shared/tests/test_dashboard_routes.py etc. green — *tests not yet written* |
| `[-]` | CLAUDE.md updated with dashboard URL — *being updated this session in Step 5* |

Plus a deferred-to-successor-plans block listing X.4 / X.5 / X.6 / Phase Y.

### §4.3 — Phase R Purpose banner (around line 3590)

Inserted "STATUS (2026-05-03): ~57% SHIPPED" callout enumerating the 7 shipped artefacts and the 6 missing artefact groups (workflow, tests, systemd units, snapshots, Makefile, heartbeat wire-in).

---

## §5 — Step 5 closeout: CLAUDE.md update

**Diff applied to [`CLAUDE.md`](CLAUDE.md:360):** appended five new status blocks before the legacy Phase 3B boundary at line 362:

1. **Phases 6-11 Implementation Status (2026-05-01)** — table of 13 components across 6 phases, all ✅ COMPLETE, with file links
2. **Phase L Implementation Status (2026-05-01)** — table of 8 dashboard components + anchor-link convention + deferred-to-successor note
3. **Phase R Implementation Status (2026-05-04)** — table of 12 components (7 ✅, 5 ❌)
4. **Phase X Implementation Status (2026-05-02 → 2026-05-03)** — table of 13 sub-phases (X.0/F1-F12 ✅, X.1 ✅, X.2-X.3 ⏳, X.4-X.6 ❌)
5. **Phase Y Implementation Status (2026-05-03)** — design complete (D1-D5 deliverables enumerated), 6 sub-phases blocked on §11

The legacy "Phase 3B" boundary remains unchanged at the bottom — anything new still must come from the unified plan, not the legacy block.

---

## §6 — Files touched this session

### Created
- `.roo/handoffs/2026-05-03-five-steps-closeout.md` — this document

### Modified
- [`shared/docs/INTELLIGENCE_UNIFIED_PLAN.md`](shared/docs/INTELLIGENCE_UNIFIED_PLAN.md:1) — Phase L purpose banner, Phase L checklist (25 items reconciled), Phase R purpose banner
- [`CLAUDE.md`](CLAUDE.md:1) — 5 new status blocks (Phases 6-11, L, R, X, Y) before legacy Phase 3B boundary

### Read but NOT modified (reference)
- [`shared/docs/PHASE_X_DASHBOARD_AND_PIPELINE_PLAN.md`](shared/docs/PHASE_X_DASHBOARD_AND_PIPELINE_PLAN.md:1)
- [`shared/docs/PHASE_X0_POSITION_PIPELINE_DIAGNOSIS.md`](shared/docs/PHASE_X0_POSITION_PIPELINE_DIAGNOSIS.md:1)
- [`shared/docs/PHASE_Y_LEARNING_DASHBOARD_PLAN.md`](shared/docs/PHASE_Y_LEARNING_DASHBOARD_PLAN.md:1) — §11 read for question surfacing
- [`shared/dashboard/server.py`](shared/dashboard/server.py:1), [`web/index.html`](shared/dashboard/web/index.html:52) — recon for X.4-X.6 status
- [`shared/scripts/`](shared/scripts/:1), [`shared/intelligence/cron_canary.py`](shared/intelligence/cron_canary.py:1), [`.github/workflows/`](.github/workflows/:1), [`systemd/`](systemd/:1) — Phase R recon
- [`.roo/handoffs/2026-05-03-master-resume-handoff.md`](.roo/handoffs/2026-05-03-master-resume-handoff.md:1) — F1-F11 cross-reference

---

## §7 — What's now blocking forward motion

Two distinct blockers, ordered by user-decision dependency:

### §7.1 — Phase Y v2: BLOCKED ON USER (5 questions in §2 above)

Until §11 answers land, Y.0-Y.5 cannot start. Estimated answer time: 5-10 minutes of user thought.

### §7.2 — Phase X.4-X.6 + Phase R closeout: NOT BLOCKED, needs Code mode

These can start immediately:

- **Phase X.4 (News Feed tab)** — 1 day per [`PHASE_X_DASHBOARD_AND_PIPELINE_PLAN.md:290-374`](shared/docs/PHASE_X_DASHBOARD_AND_PIPELINE_PLAN.md:290)
- **Phase X.5 (Cross-Tab Drawer)** — ½ day per [`PHASE_X_DASHBOARD_AND_PIPELINE_PLAN.md:376-422`](shared/docs/PHASE_X_DASHBOARD_AND_PIPELINE_PLAN.md:376)
- **Phase X.6 (Config tab)** — 1 day per [`PHASE_X_DASHBOARD_AND_PIPELINE_PLAN.md:422-487`](shared/docs/PHASE_X_DASHBOARD_AND_PIPELINE_PLAN.md:422)
- **Phase R closeout** — 1 day per §3.2 above (workflow + tests + systemd + snapshots + Makefile + heartbeat wire-in)

Total: 3.5 days Code mode work to fully close out the dashboard surface and CI enforcement layer.

---

## §8 — Resume command for next session

> "Read [`.roo/handoffs/2026-05-03-five-steps-closeout.md`](.roo/handoffs/2026-05-03-five-steps-closeout.md:1).
> If the user has answered §2's five PHASE_Y v2 questions, switch to Code mode and start Y.0 (MemU enum extension).
> Otherwise, surface §2 to the user verbatim and wait for answers. While waiting, optionally start Phase R closeout (§3.2 priority order: GH Actions workflow → 4 tests → 3 systemd units → snapshots → Makefile → heartbeat wire-in) since it's not user-blocked."

---

## §9 — Sign-off

**Architect mode, 2026-05-03T10:26 UTC.** All five recommended next-steps from the implementation-progress report executed end-to-end:

- ✅ Step 1 — X.4-X.6 verdict definitive (unshipped)
- ✅ Step 2 — PHASE_Y §11 surfaced
- ✅ Step 3 — Phase R closeout spec written
- ✅ Step 4 — INTELLIGENCE_UNIFIED_PLAN.md reconciled
- ✅ Step 5 — CLAUDE.md updated
- ✅ Step 6 — this handoff

Awaiting user response on PHASE_Y §11 before Y.0 can begin. Code mode can independently begin Phase R closeout or Phase X.4 in the meantime.
