[pytest]
asyncio_mode = auto
testpaths = shared/tests
markers =
    integration: requires running services (Qdrant, Postgres)
    slow: marks tests as slow (deselect with '-m "not slow"')
    asyncio: mark test as async
