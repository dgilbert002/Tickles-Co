# ═══════════════════════════════════════════════════════════════════
# JarvAIs Dependencies
# Database (MySQL): host, user, password, database name are in config.json
# under global.database (see config.example.json). Do not commit real passwords.
# ═══════════════════════════════════════════════════════════════════

# --- Core Framework ---
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
flask>=3.1.0
jinja2>=3.1.0

# --- Database ---
pymysql>=1.1.0
cryptography>=43.0.0
dbutils>=3.1.0

# --- AI Model APIs ---
anthropic>=0.40.0
openai>=1.55.0
google-generativeai>=0.8.0

# --- Vector Database ---
qdrant-client>=1.12.0

# --- Memory Layer ---
mem0ai>=0.1.0

# --- Local Embeddings (free, offline) ---
sentence-transformers>=2.2.0

# --- Pinecone (optional, for cloud vector store) ---
pinecone-client>=3.0.0

# --- MetaTrader 5 (Windows only — install separately on VPS) ---
# MetaTrader5>=5.0.45

# --- Data Processing ---
pandas>=2.2.0
numpy>=1.26.0

# --- Technical Analysis ---
# TA-Lib: Required. Install the C library first, then: pip install TA-Lib
# Windows: download pre-built wheel from https://github.com/cgohlke/talib-build/releases
# Linux:   apt install libta-lib-dev && pip install TA-Lib
TA-Lib>=0.4.0
# pandas-ta>=0.3.14b  # optional; install from conda-forge or pandas-ta.dev if needed

# --- Telegram (authorized access) ---
telethon>=1.37.0

# --- Discord (user-token self-bot; token via tools/get_discord_token.py) ---
# MUST be discord.py-self (PyPI: discord.py-self). Standard discord.py is bot-token only.
# If you have 'discord.py' installed, uninstall it: pip uninstall discord.py
discord.py-self>=2.0.0
# curl_cffi >=0.15.0 required: 0.13.0 had fatal C-level crash (ffi.from_handle garbage)
# during Discord websocket reconnection that killed the entire process.
curl_cffi>=0.15.0
PyNaCl>=1.5.0  # Required for Discord voice support

# --- Exchange connectivity (CCXT Pro included in CCXT) ---
ccxt>=4.0.0

# --- News & RSS ---
feedparser>=6.0.0
aiohttp>=3.10.0
requests>=2.31.0

# --- Headless browser (Idea Monitor screenshots; run "playwright install chromium" after pip install) ---
playwright>=1.49.0

# --- Discord token setup (tools/get_discord_token.py: opens browser, extracts token) ---
selenium>=4.18.0
webdriver-manager>=4.0.0

# --- Charting & Visualization ---
plotly>=5.24.0

# --- Utilities ---
psutil>=5.9.0
python-dotenv>=1.0.0
pydantic>=2.9.0
httpx>=0.27.0
apscheduler>=3.10.0

# --- Market Data (VIX via Yahoo Finance) ---
yfinance>=0.2.37
